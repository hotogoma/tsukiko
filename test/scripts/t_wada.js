const assert = require('assert');
const bot = require('../..');

describe('t_wada', () => {
  context('when receive "テスト書いてない"', () => {
    it('should send t_wada message', () => {
      // TODO
    });
  });
});
